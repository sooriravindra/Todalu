#!/usr/bin/env todalu
# ^ May have to fix the path
(def fzbz (lambda (y)
            (if (eq? (% y 15) 0)
                (println "FizzBuzz")
                (if (eq? (% y 5) 0)
                    (println "Buzz")
                    (if (eq? (% y 3) 0)
                        (println "Fizz")
                        (println y))))))
(map fzbz (range 1 101))
