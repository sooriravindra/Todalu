#!/usr/bin/env todalu
# ^ May have to fix the path
(def fib (lambda (a b count)
           (if (<= count 0)
               t
               (progn
                 (println a)
                 (fib b (+ a b) (- count 1))))))
(fib 0 1 20)
